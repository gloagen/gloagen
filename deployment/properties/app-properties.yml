artifactory:
    user: 'cariflo'
    password: 'p5QUTcO0'
    repository:
        release:
            url: 'http://mayze.internal-1.uk:8081/artifactory/libs-release-local/{groupid-directory}/{app-name}/{version}/{app-name}-{version}.{type}'
archive:
    download:
        chunk_size: 2048
        cleanup:
            before: true
            after: true
tomcat:
    webapps:
        backup:
            enabled: false
            directory:
