environment:
    variables:
        DEPLOY_ROOT: '/opt/codedeploy-agent/deployment-root/gloagen-app'
        DEPLOY_LOGS_DIR_NAME: 'logs'
        WEBAPPS_HOME: '/opt/server/tomcat/webapps'
        DEPLOY_PROPERTIES_DIR: 'properties'
        DEPLOY_RELEASE_DIR: 'release/userservice
        export GLOAG_DEPLOY_RELEASE_PROPERTIES_HOME=${GLOAG_DEPLOY_RELEASE_HOME}/properties
        export GLOAG_DEPLOY_DOWNLOADS_HOME=${GLOAG_DEPLOY_HOME}/downloads
artifactory:
    user: 'cariflo'
    password: 'p5QUTcO0'
    repository:
        release:
            url: 'http://mayze.internal-1.uk:8081/artifactory/libs-release-local/{groupid-directory}/{app-name}/{version}/{app-name}-{version}.{type}'
archive:
    download:
        chunk_size: 2048
        cleanup:
            before: true
            after: true
tomcat:
    webapps:
        backup:
            enabled: false
            directory:
